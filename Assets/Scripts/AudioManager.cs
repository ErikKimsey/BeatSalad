using System.Collections;
using System.Collections.Generic;
using UnityEngine.Audio;
using UnityEngine;

public class AudioManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /**
    - if audiobox is touching startbox, then that audio box unmutes.
    - subsequent audioboxes that are either touching a.) the startbox or b.) an audiobox that is already touching the startbox, are then unmuted.
    */
}
